version: '3.8'
services:
  api:
    image: a2a-client-api:${APP_VERSION:-v1}
    build: ./backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    networks:
      - app-network

  web:
    image: a2a-client-web:${APP_VERSION:-v1}
    build:
      context: ./frontend
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3001}
    ports:
      - "80:80"
    depends_on:
      - api
    networks:
      - app-network

  mcp:
    image: a2a-client-mcp:${APP_VERSION:-v1}
    build: ./mcp-server
    ports:
      - "8081:8081"
    environment:
      - WEBHOOK_URL=http://api:3001/webhook
      - MCP_PORT=8081
    depends_on:
      - api
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
